# SPEC — Christina Sun Photography

A static photography portfolio generated by a Python build script, replacing the Squarespace 7.0 site (Pacific template) at christinasunphoto.com.

This file is the source of truth for *what to build*. `DECISIONS.md` records *why* each choice was made, with dates. `TODO.md` records what is still open. When the three disagree, fix the disagreement before building.

I'm learning Python (coming from Stata): write every script in a clean, well-commented, readable style I can learn from. Prefer clarity over cleverness. Name the source of any path, value, or convention you derive from the repo or the old site.

The quality bar: this must look like a site made by a professional design studio for a working photographer, at least as polished as a premium Squarespace portfolio template, and faster. If any page reads as templated, bootstrap-ish, or obviously AI-generated, iterate before calling it done.

## Site structure

Navigation contains exactly five items, in this order:

1. **Landscape** — landscape series, currently film and 2019 digital.
2. **People** — commissioned portrait, couple, and graduation work.
3. **Interludes** — personal film that is neither commissioned nor landscape: beach days, friends, quiet rolls.
4. **Archive** — older street and personal-project work, reached only through one quiet index page.
5. **About** — portrait, bio, exhibitions, contact.

The landing page is a full-viewport crossfading slideshow with the wordmark and tagline over it. It is the site's thesis.

Section names come from the tagline: nature, people, and everything in between.

### The series is the content unit

Every gallery on the site is a *series*: a titled, sequenced body of work with one coherent aesthetic, a short statement, a cover, and a tone (color or monochrome). Series are never grouped by trip, camera format, or year. Color versus monochrome is a tag on the series, never a level in the hierarchy. A section index page is a grid of series covers, so no single page ever mixes eras or palettes.

Series can be added, retitled, reordered, or moved between sections by editing one YAML file. Nothing in the templates or build script hard-codes a series.

### Launch content

Everything below already exists on the Squarespace site and is migrated by script (see Migration). Titles marked *placeholder* are open questions in `TODO.md`.

| Section | Series title | Source slug on Squarespace | Images | Notes |
|---|---|---|---|---|
| Landscape | *Alpenglow* (placeholder) | `landscape-color` | 12 | 2019 digital. Drop `000033810003-Edit.jpg`, which is film and already opens *The Mountains are Calling*. |
| Landscape | *Storm Light* | `landscape-monochrome` | 6 | 2019 digital, Glacier National Park. Per-image titles exist in the alt text; keep as captions. |
| Landscape | Sand and Stone | `sand-and-stone` | 9 | 2024 film, monochrome. |
| Landscape | The Mountains are Calling | `from-the-mountains-to-the-sea` | 6 | 2020 color film. Candidate for merging; see `TODO.md`. |
| People | From the Earth to the Stars | `from-the-earth-to-the-stars` | 42 | |
| People | An Instant of Forever | `instant-of-forever` | 12 | |
| People | Love on the Coast | `love-on-the-coast` | 33 | |
| People | Tide and Timber | `tide-and-timber` | 21 | Polaroids. |
| People | Graduations | `graduations` | 23 | |
| Interludes | Quiet Days | `quiet-days` | 6 | |
| Archive | Meditation on Solitude | `meditation-on-solitude` | 14 | |
| Archive | Nightfall | `nightfall` | 12 | |
| Archive | Outskirts | `outskirts` | 13 | |
| Archive | Panoramic Streets: Monochrome | `panoramic-streets-monochrome` | 19 | |
| Archive | Panoramic Streets: Color | `panoramic-streets-color` | 6 | |
| Archive | Animal Companions | `animal-companions` | 2 | |
| Archive | The City by the Sea | `sanfrancisco` | 3 | |
| Archive | UC Davis Symphony Orchestra | `uc-davis-symphony-orchestra` | 18 | |
| Archive | Streets of Sacramento | `new-gallery-1` | 20 | |
| (unpublished) | Streets of Davis | `streets-of-davis` | 15 | Orphaned on the old site. Migrate with `published: false`; decide later. |
| (unpublished) | Life and Adventure | `life-and-adventure` | 5 | Same. |
| (unpublished) | Adventure | `adventure` | 5 | Same. |

Image counts are unique images from `sitemap.xml`; the sitemap lists each gallery's cover twice, so the migration script must de-duplicate.

Not migrated: the blog (one 2019 post), the demo print store (lorem ipsum placeholders), the `/home`, `/landscape`, and `/anthology` duplicate pages, the newsletter form, the Facebook link, and the empty `jmt-2025` and `clouds-rest` blog collections.

## Tech constraints

- Output is plain static HTML, CSS, and vanilla JavaScript. No React, no Node toolchain, no CMS, no database.
- Python 3.11+ build script using Jinja2 (templates), Pillow (images), and PyYAML. `requirements.txt`; venv documented in README.
- Build output in a gitignored `dist/`.
- Photographs are committed to git alongside their YAML; the repository is the complete source of truth and a fresh clone builds the whole site.
- Deploy via GitHub Pages with a GitHub Actions workflow and a `CNAME` file for `christinasunphoto.com`.
- The only frontend dependency is PhotoSwipe, vendored into the repo. Zero external requests; fonts self-hosted.
- Contact form posts to a Formspree action URL left as a clearly marked placeholder.
- No analytics, no cookies, no trackers, no newsletter.

## Repo structure

```
content/
  site.yaml                  # global config
  series/
    <slug>/
      series.yaml            # per-series metadata and image order
      *.jpg                  # web-res exports (source of truth)
  about/
    about.md
    portrait.jpg
templates/                   # Jinja2
static/                      # css, js, fonts, PhotoSwipe, favicons
scripts/
  build.py                   # entry point: build, --serve
  migrate_squarespace.py     # one-time import from the live Squarespace site
  make_favicons.py           # generates static/favicon.* once
  photosite/                 # the build package: content, images, render, serve, curate
dist/                        # generated site (gitignored)
.cache/                      # image-processing cache (gitignored)
.github/workflows/deploy.yml
README.md
SPEC.md                      # this file
DECISIONS.md                 # dated log of decisions and rationale
TODO.md                      # open questions and future work
```

## Content model

### `site.yaml`

- `title`: `Christina Sun`
- `tagline`: `Wild and human-altered landscapes documenting the tales of nature, stories of people, and everything in between.`
- `description`: a new one-paragraph site description. Do not copy the old Squarespace description ("Landscape, Travel, and Street Photography..."), which is stale.
- `nav`: the five sections above, with the slug and label for each.
- `instagram`: `https://instagram.com/chesunphotography` (from the current site footer and cover page).
- `email`: `christinasunphotography@gmail.com`. This is the canonical address (decided 2026-09-13). The old cover page linked `chesunphotography@gmail.com`, which also exists but is not used on the site.
- `base_url`: `https://christinasunphoto.com`
- `formspree_action`: placeholder.

### `series.yaml`

```yaml
title: Sand and Stone
section: landscape          # landscape | people | interludes | archive
order: 30                   # position within the section index; lower first
tone: monochrome            # color | monochrome
layout: column              # column | grid   (default column)
published: true             # false = keep the files, build nothing
statement: >
  Photos from my January 2024 trip to the Eastern Sierra and Death Valley.
  A variety of B&W film stocks.
cover: 000038580006.jpg
featured:                   # optional; images for the landing slideshow
  - 000038580006.jpg
images:                     # display order; this list IS the sequence
  - 000038580006.jpg
  - 000038570010.jpg
captions:                   # optional, filename -> caption
  000038580006.jpg: Alabama Hills
focal:                      # optional, filename -> CSS object-position for cropped uses
  000038580006.jpg: 50% 30%
```

Rules the build enforces:

- `images` is the display order. The curate page edits it; hand-editing is fine too.
- An image file in the folder that is not listed in `images` is appended to the end with a warning, so a fresh Lightroom export shows up without editing YAML.
- An unpublished series may be empty (the curate page creates them that way); a published one needs at least one image.
- An image listed in `images` that is missing from the folder fails the build with a message naming the series and file.
- `cover` must be in `images`. `featured` entries must be in `images`.
- `section` must be one of the four. `tone` and `layout` must be one of their allowed values. Any other key is an error, so typos surface.
- Non-image files in a series folder (other than `series.yaml`) fail the build.

## Build script behavior (`python scripts/build.py`)

- For every source image generate resized variants: thumbnail (480px long edge), small (1000px, what a 3x phone picks), medium (1600px), large (2400px, saved at a slightly lower JPEG quality because it is the heaviest and what retina desktops pick). Output names carry a short content hash so a re-export gets a new URL. Emit `srcset`/`sizes`; lazy-load below the fold; only the first frame gets `fetchpriority=high`.
- **Record each image's pixel dimensions and average color at build time** and bake them into the HTML: every image gets an exact `aspect-ratio` box with its average color as the background, so the page never shifts layout and photos fade in over a matching tone while loading. This is required; it is most of what makes a photo site feel expensive.
- Strip EXIF (including GPS) from output images; preserve orientation; convert to sRGB.
- Incremental builds: cache by content hash (plus a hash of the variant settings) in `.cache/`; unchanged images are never reprocessed. `dist/` is deleted and rebuilt on every build, with variants hard-linked from the cache, so rebuilds with no image changes stay near-instant and nothing stale survives. Swapping a CDN copy for a Lightroom re-export of the same frame is just a new hash.
- Generate `sitemap.xml` (URLs only; no lastmod, since file times mean nothing in a fresh checkout), a designed 404 page, favicon + apple-touch-icon, and per-page meta/OpenGraph tags (og:image = series cover; a series without a statement gets a generated description).
- `--serve`: local dev server with rebuild-on-change (a simple polling watcher is fine), restarting itself when a build script changes. Also serves the curate page at `/_curate/`.
- Fail loudly and helpfully on bad YAML, missing files, unknown keys, or non-image files.

## Migration script (`python scripts/migrate_squarespace.py`)

Run once, before the Squarespace subscription is cancelled. After that the Squarespace CDN is gone and this script is dead code kept for the record.

- A mapping dict at the top of the script: source slug → target series slug, section, order, tone, title, published. The table in Launch content is that dict.
- For each source slug, fetch `https://christinasunphoto.com/<slug>`, parse the gallery slides in page order, and take each slide's `data-src`. Page order on Squarespace is the display order and must be preserved into `images`. De-duplicate (the cover appears twice).
- Download each image from its **bare CDN URL with no `?format=` parameter**. Verified on 2026-09-13: the bare URL returns the stored upload (2500px long edge, ~300KB); `?format=2500w` returns a recompressed 160KB copy; `?format=original` is identical to bare.
- Keep the original filename. Squarespace filenames with `+` decode to spaces; replace spaces with `-`.
- `statement` comes from the page's `<meta name="description">` when present (Sand and Stone, The Mountains are Calling, Love on the Coast, Tide and Timber, Meditation on Solitude, Nightfall, Outskirts have real ones; Landscape: Color and Monochrome have descriptions that should be discarded with the retitling).
- Per-image alt text that is not a filename (the monochrome series has titles like "Impending Storm") becomes a `captions` entry.
- For `landscape-color`, skip `000033810003-Edit.jpg`.
- Seed `content/about/about.md` from the current About page: the bio paragraphs, the sign-off, the Dorothea Lange quote, the "Exhibitions/Features" list with its link targets, and the academic-site link. Download the About page portrait as `portrait.jpg`.
- Idempotent: skip files that already exist, never overwrite a `series.yaml` that has been edited.

## Curate page (local only)

Served by `--serve` at `/_curate/`, never written to `dist/`. Set in the site's own type and palette. It replaces the one thing Squarespace did well, arranging photos by eye, and the ordinary content chores around it.

- One card per series, grouped by section, published or not, thumbnails about 136px tall. Drag to reorder with a before/after drop indicator; arrow keys move a focused photograph.
- Per photograph: cover, feature (landing slideshow), focal point, and a menu to move it to another series or remove it. Captions and focal points travel with a moved photograph. Removed photographs go to `content/_removed/<series>/`, never straight to deletion.
- Focal-point tool: the frame large, a draggable ring for the anchor, the desktop (16:9) or phone (9:16) crop window drawn on the photograph itself, arrow-key nudging, Enter to accept.
- Landing strip at the top: the slideshow in running order (section, series order, featured order), each slide cropped at its focal point; click to adjust. This is where the review-by-eye pass happens.
- Add photos: JPEG upload into a series folder, appended to the order, non-JPEGs refused, name collisions suffixed. Holding section: files under `content/_removed/` and `content/_unplaced/`, to place into a series or delete permanently (with confirmation). New series: title, section, tone, order; creates an empty, unpublished `series.yaml`.
- Save writes `images`, `cover`, `featured`, and `focal` by replacing those blocks in the YAML text, so every other key and comment survives; the server validates the result strictly and, if it would not load, restores the previous text and refuses, so a refused write leaves nothing behind. Removing the last photograph of a published series is refused (unpublish it first). Requests are accepted only from the page itself (same origin, JSON body). The dev server's watcher then rebuilds.
- Thumbnails and medium copies come from the image cache through a `/_curate/img/` route. No dependencies beyond the standard library, Pillow, and PyYAML.

## Design system

Design vision: a quiet, gallery-white, editorial site where the design disappears behind the photographs. Every visible element — type, spacing, motion — should feel deliberate and calm. Minimalism here means precision, not emptiness: the polish lives in spacing rhythm, typographic detail, and how images load and move.

**Color.** Near-white background (in the neighborhood of `#FBFBF9`), near-black text (`#1A1A1A`), one mid-gray for captions and metadata. No accent color, no borders or shadows on photographs. The lightbox and the landing slideshow are the one dark context: near-black (`#0E0E0E`).

**Typography.** Two self-hosted open-source faces, subset to the characters used, `font-display: swap`:

- **Inter** (chosen 2026-09-14 from a comparison sheet of nine candidates), weights 300 and 400, for the wordmark, nav, captions, and body. Wordmark and nav are uppercase with generous letterspacing (tracking ≥ 0.15em) at small sizes. This is the one thread of continuity with the current site's identity; the current face is Futura PT, which is not self-hostable and does not need to be matched.
- **Newsreader** (chosen the same day from ten candidates), weight 300 with its optical-size axis left on automatic, plus the 300 italic for the landing tagline, for series titles, statements, and the About headline, used sparingly and large.

Set a real type scale with intentional sizes and weights; body text max-width around 65ch. Type is the personality of the page; make the pairing feel chosen for a photographer, not defaulted.

**Layout.** An 8px spacing scale used consistently. Header is minimal: wordmark left, five nav items right, a near-white bar with a subtle backdrop blur on every inner page. The landing page has no header; its navigation lives in the hero (see Landing slideshow). Mobile: a full-screen overlay menu with the same restraint. Footer is one quiet line (name, Instagram, email).

### Series pages: column layout (default)

Visual impact over density. One photograph per row, in sequence, so the order chosen in the curate page is the experience.

- Each image is sized so it fits the viewport: width up to the content column (about 1600px on large screens), height capped at about 88vh. A panorama runs the full column width; a square or portrait frame stands nearly as tall as the screen. On phones every image is full width minus the 16px gutters.
- Generous vertical space between frames: 96 to 128px on desktop, 48px on mobile, from the 8px scale. The space is part of the sequence.
- The optional statement sits above the first image, in the serif, short and centered.
- Captions, when present, sit under the image in the grotesque at caption size, gray, left-aligned to the image.
- Every frame still gets the aspect-ratio box and average-color fade-in.
- Click opens the lightbox at that image for true full-screen and keyboard navigation, but the column is the primary viewing experience, not the lightbox.

### Series pages: grid layout (opt-in)

For high-volume galleries where scanning matters more than sequence, `layout: grid` gives a justified-row layout computed at build time from the known aspect ratios (rows of consistent height, images scaled to fill the row width exactly, last row left-aligned at natural size). No JavaScript layout, no masonry gaps. Hover: a subtle opacity ease (~0.92, 200ms), never a zoom-crop. No launch series uses this; it exists so Graduations or Love on the Coast can switch to it later with one line.

### Section index pages (Landscape, People, Interludes)

Series covers in justified rows computed at build time: covers are added to a row until, at a target height of about a third of the content width, they would fill it, then the row's height is solved so the covers plus gaps fill the width exactly. Every cover keeps its own aspect ratio, every image in a row has the same height, and titles line up. A trailing partial row keeps the target height rather than stretching. Under each cover, the series title in the serif and a small uppercase tone tag (Color or Monochrome) in the grotesque. Ordered by `order`. On phones the rows stack to one full-width cover each.

### Archive index

The same justified rows at a smaller target height, about a fifth of the content width, with title and tone tag. Archive series are never in the nav and never in section indexes.

### Landing slideshow

Full-viewport crossfading slideshow of every image marked `featured` across all published series, in section order (Landscape, People, Interludes, Archive), then series order, then the order of each `featured` list: ~6s hold per image, ~1.4s opacity crossfade, and a barely-perceptible slow scale (1.0 → 1.04 over the hold) for life. First slide ships as a high-priority preload; subsequent slides load just-in-time. Each slide is cropped to the viewport at its `focal` point from `series.yaml` (CSS `object-position`, default centre), so a tall frame can keep its sky or its ground. The site header is not shown on the landing page: the wordmark, the tagline (serif italic), and the five section links sit centred over the image, inside one soft radial scrim, so the navigation never competes with a light sky. A quiet line with Instagram and email sits at the foot. Pause the cycle when the tab is hidden. Under `prefers-reduced-motion`, show a single static image.

**Motion.** One orchestrated moment per page, not scattered effects: page content fades in briefly on load; column and grid items reveal on scroll via IntersectionObserver with a subtle staggered fade-up (opacity + ~8px, ~300ms). All motion uses gentle easing and is fully disabled under `prefers-reduced-motion`.

**Lightbox.** Skin PhotoSwipe to match: near-black backdrop, minimal chrome, captions set in the grotesque at caption size, keyboard and swipe navigation. It should feel native to the site, not like a plugin.

**Micro-detail floor (non-negotiable):** visible `:focus-visible` states; a styled text-selection color; ≥44px touch targets (every link, footers included); muted text at or above 4.5:1 contrast; active-section indication in the nav; a designed (not default) 404; correct favicons; no layout shift anywhere (verify with the aspect-ratio boxes above).

**Favicon.** A near-black disc on the site's off-white, a sun for Sun: `favicon.svg` for modern browsers, `favicon.ico` at 16, 32, and 48px, and a 180px `apple-touch-icon.png`. Generated once by `scripts/make_favicons.py` and committed under `static/`.

**Performance.** Target Lighthouse ≥ 95 on performance, accessibility, best practices, and SEO for a series page. Preload the hero image and fonts; everything else lazy. Column layout downloads bigger images than a grid would, so lazy loading and the medium variant's size matter more here.

## Pages

- **Landing**: the slideshow, wordmark + tagline overlay, transparent nav.
- **Section index** (Landscape, People, Interludes): grid of series covers as above.
- **Series page**: optional statement, then the column (or grid), opening into the lightbox.
- **Archive index**: small covers + titles linking to archived series pages.
- **About**: portrait, bio from `about.md`, exhibitions list, academic-site link, email + Instagram, minimal contact form (Formspree placeholder). No newsletter.
- **404**: designed.

## Phases and definition of done

Build in four phases. Each phase ends with a full build, `--serve`, screenshots of every page type, a written self-critique against the design system (spacing, type, motion), and a walkthrough for me. Do not start the next phase until the current one clears the quality bar.

1. **Content and series pages.** Content model, `migrate_squarespace.py` run against the live site, image pipeline with variants, EXIF stripping, hash cache, aspect-ratio boxes and average color, series pages in column layout, lightbox, header and footer, `--serve`. Verified with the real migrated content, not placeholders.
2. **Site chrome.** Landing slideshow with scrim and reduced-motion fallback, section indexes, Archive index, About, 404, sitemap and OpenGraph tags, favicons, motion, mobile menu. Grid layout implemented and tested on one series, then switched back to column.
3. **Curate page.** Reorder, cover, featured, focal points, landing preview, save. Verified by reordering a series through the running server and checking the rebuilt page.
4. **Deploy.** GitHub Actions workflow (`.github/workflows/deploy.yml`: build on push to `main`, cached image variants, publish `dist/` to GitHub Pages), `CNAME` and `.nojekyll` written by the build, README deploy steps, DNS instructions for a domain registered at Squarespace. Lighthouse was run during the review pass (mobile series page 85 performance, 100 accessibility, best practices, and SEO).

## README

Step-by-step: setup (venv, install, build, serve); the content model; adding a series; the curate page; retitling or moving a series between sections; unpublishing a series; the Lightroom export settings to use (~2500px long edge, JPEG ~80, sRGB); running the migration once; deploying; keeping the domain at Squarespace as domain-only and pointing its DNS at GitHub Pages.

## Out of scope for now

Tracked in `TODO.md`, not built: the iPhone trip journals on christinasun.net, redirects from old Squarespace URLs, transferring the domain away from Squarespace, journal text from the trail notebooks, newsletter, blog, print store, Facebook.
